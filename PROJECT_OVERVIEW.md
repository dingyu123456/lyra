# Lyra 多集群 GPU 智能调度器 — 项目亮点梳理

## 一、项目定位与背景

Lyra 是一个构建在 Karmada 之上的多集群 GPU 智能调度器，面向 AI 训练和推理场景，解决多集群环境下 GPU 资源的精细化调度问题。

### 为什么做多集群 GPU 调度

- **K8s 原生局限**：K8s 调度器只支持单集群调度，无法满足企业多集群管理需求
- **GPU 虚拟化趋势**：HAMI 等项目实现了 GPU 虚拟化（将一张物理 GPU 切分为多个 vGPU），支持精细化调度
- **多集群现实需求**：企业普遍采用多集群部署，未来随着模型参数增大，可能出现跨数据中心的分布式训练场景（对网络要求极高，需全光网支撑）
- **Karmada 的不足**：Karmada 是目前最广泛使用的多集群调度管理平台，但其调度时只考虑 Cluster 级别的资源剩余量，且对 GPU 资源的统计存在根本性缺陷

### 为什么选择构建在 Karmada 之上

利用 Karmada 现成的子集群连接和任务下发能力（PropagationPolicy / OverridePolicy），避免重复造轮子。我们的调度器专注于**调度决策**（选出最优集群、节点、GPU），决策结果通过 Karmada 的下发通道执行。

---

## 二、亮点一：基于 Karmada 的多集群 GPU 精细化调度架构

### Karmada 原生 GPU 调度的缺陷

Karmada 汇聚的 GPU 信息是**不准确的**，核心原因在于 GPU 的物理隔离特性：

- 一张物理 GPU 是物理隔离的，无法将一个 vGPU 的需求拆分到多张卡上
- 例如：一个任务需要 0.5 张卡（即申请一个 vGPU），只能整块部署到某一张卡的 0.5，**不可能**一张卡部署 0.2、另一张部署 0.3
- 只有当一个任务申请**多个 vGPU** 时，才可以分配到多个物理 GPU 上
- Karmada 原生将 GPU 资源当做普通标量聚合（如把 3 张卡的显存加在一起），忽略了单卡的物理隔离约束，导致统计和调度结果都有误

### Lyra 的解决方案

维护一个**调度器级别的内存缓存**，精确追踪每一块 GPU 的 UUID、显存、算力百分比、可切分 vGPU 数量等细粒度信息。

### 双层连接架构

调度器需要建立两层连接：

**第一层：连接 Karmada 控制面**
- 监听 Cluster 资源变化，感知子集群的接入/移除
- 监听未调度的 Pod，将其推入调度队列
- 通过 Karmada 下发调度决策（创建 PropagationPolicy / OverridePolicy）

**第二层：直连所有子集群**
- 当 Karmada 监听到新集群接入时，通过 ClusterAccessManager 自动获取子集群的 Kubeconfig，建立该集群的 Informer 监听
- 监听子集群的 **Node 事件**：HAMI 会将每个节点的 GPU 总量信息（每块 GPU 的 UUID、显存、算力百分比、可切分 vGPU 数量）写入 Node 的 Annotation 中，我们通过解析 Annotation 获取 GPU 资源数据
- 监听子集群的 **Pod 事件**：HAMI 给 Pod 分配具体 GPU 后，会将 GPU 的 UUID、显存、算力百分比等信息写入 Pod 的 Annotation 中，我们通过解析这些信息做资源扣减和缓存更新

### 调度流程

```
Pod 进入调度队列
    |
    v
PreFilter（解析 GPU 需求、集群剪枝）
    |
    v
Filter（并发过滤满足条件的节点，含 GPU 硬约束校验）
    |
    v
Score（并发打分，含 GPU 评分 + 节点资源评分）
    |
    v
GPU 分配（Best-Fit 二维装箱，选出具体物理卡 UUID）
    |
    v
缓存预扣（AssumePod）
    |
    v
通过 Karmada 下发到目标子集群
```

---

## 三、亮点二：调度器缓存与资源预扣机制

### 问题背景

在之前的调度系统中，我们使用 Informer 事件的回调函数实时将资源剩余量写入 MySQL，调度时从 MySQL 读取数据。这存在两个严重问题：

**问题 1：不必要的网络 IO**
- 一般云原生项目为了减少网络 IO 都维护内存缓存
- 我们通过 Informer 回调写 MySQL，调度时再从 MySQL 读，多了两次网络 IO

**问题 2：资源数据滞后导致超卖**
- Pod 调度下去后并不是瞬间占用资源的，从下发到真正运行再到 Informer 事件更新，可能需要数秒
- 当两个相同资源请求的并发任务到达时，第一个任务调度后 Informer 事件还未更新资源剩余量，调度器就开始调度第二个任务，可能导致同一块 GPU 被超卖给两个任务

### 解决方案：参考 K8s 的预扣（Assume）机制

参考 K8s 调度器源码中的 Scheduler Cache 设计，实现内存缓存 + 乐观预扣：

**预扣流程**：
1. 调度算法（Filter → Score）选出最优节点和 GPU 后，在真实下发之前，先在内存缓存中**乐观地预扣**资源
2. 预扣时将调度决策（集群、节点、GPU UUID）写入 Pod 的 Annotation
3. 预扣完成后，调度器可以继续处理下一个 Pod，不用等待绑定完成

**TTL 超时回滚**：
- 预扣的 Pod 设置 TTL（如 30 秒），后台协程定期清理过期未转正的预扣
- 如果 Binding 还在进行中，则跳过清理，防止因 API 调用慢导致资源被错误释放

### 预扣转正的对账机制

光有预扣还不够——Pod 下发后不一定能成功占用资源启动，需要监控实际运行情况：

1. **监听子集群 Pod 事件**：通过直连子集群的 Informer，当 Pod 成功绑定到 Node 后会推送事件
2. **身份校验**：检查该 Pod 是否在缓存的预扣名单中
3. **对账比对**：比较调度器之前为该 Pod 分配的节点和 GPU，与该 Pod 实际绑定的节点和 GPU 是否一致
   - **一致**：将预扣状态转正，资源扣减生效
   - **不一致**：取消预扣，按照实际使用的资源重新扣减，保证数据一致性

### 为什么只对接 Karmada 不够

对接 Karmada 只能实现下发动作，监控不到具体 Pod 的事件。所以必须直连子集群，形成"调度 → 预扣 → 下发 → 监控 → 对账转正/回滚"的闭环。

### 二维 MRU 增量快照

缓存内部采用**二维双向链表 + 世代号（Generation）**的增量更新机制：

- **第一维**：集群级别的 MRU 链表，资源发生变动的集群移到链表头部
- **第二维**：节点级别的 MRU 链表（挂在每个集群内部），资源变动的节点移到头部
- **增量快照**：调度时从缓存克隆快照，利用世代号实现 O(变化量) 的增量拷贝。只要集群/节点的世代号 <= 快照世代号，说明没有变化，直接跳过

这样即使集群中有成千上万个节点，每次快照也只拷贝发生变化的少量数据。

---

## 四、亮点三：基于 QueueingHint 的智能唤醒机制

### 问题背景

多集群环境下 Pod 数量可能很大，不可调度 Pod 如果每次都全量重试，调度器压力会非常大，且大量重试是无效的。

传统的指数退避（Exponential Backoff）只能防止惊群效应，无法避免无效重试。

### K8s 的 QueueingHint 机制

参考 K8s 调度器源码中的 QueueingHint 机制实现智能唤醒：

**核心思想**：不满足调度条件的 Pod 被放入不可调度池时，会记录**是哪个插件拒绝了它**。只有当集群中发生了可能让该 Pod 满足条件的事件时，才将它重新放入调度队列。

**三级队列结构**：

| 队列 | 用途 |
|------|------|
| ActiveQ（活跃队列） | 存放随时可以调度的 Pod，按优先级排序 |
| BackoffQ（退避队列） | 存放刚调度失败的 Pod，按退避到期时间排序 |
| UnschedulablePods（不可调度池） | 存放因资源不足被拒绝的 Pod |

**智能唤醒流程**：

1. **记录拒绝原因**：Pod 在 Filter/Score 阶段被某个插件拒绝时，连同插件名称一起记录到 Pod 的"病历本"（UnschedulablePlugins）中
2. **插件注册感兴趣的事件**：每个插件声明自己关心哪些集群事件（如 NodeAdd、PodDelete、GPU 资源变化等）
3. **事件触发时精准评估**：
   - 第一道闸门：检查这个事件是否在任何插件的关心列表中，不关心直接跳过
   - 第二道闸门：遍历不可调度池中的 Pod，只评估那些曾经被关心此事件的插件拒绝过的 Pod
   - 第三道闸门：调用插件的 QueueingHintFn，让插件自己判断"这个事件是否可能让该 Pod 满足条件"
4. **策略分流**：
   - `QueueSkip`：没救，继续在不可调度池等待
   - `QueueAfterBackoff`：有可能，放入退避队列，等惩罚时间过了再进活跃队列
   - `QueueImmediately`：立刻放入活跃队列（如 Gang 调度的兄弟到齐了）

### In-Flight 并发竞争防护

当 Pod 离开队列正在被调度时，如果集群发生了资源释放事件：
- 通过 `moveRequestCycle` 记录最近一次资源变动时的调度周期号
- Pod 调度失败回来时，如果发现期间有资源变动，直接放入退避队列而不是不可调度池，确保不会因为时序问题错过调度机会

### 兜底机制

- 退避队列中的 Pod 惩罚时间到期后，自动刷入活跃队列
- 不可调度池中超过 5 分钟（可配置）的"钉子户"，强制刷入退避队列重试

---

## 五、整体数据流闭环

```
                         Karmada 控制面
                              |
          +-------------------+-------------------+
          |                   |                   |
    Cluster 事件          Pod(未调度)         调度决策下发
          |                   |                   |
          v                   v                   |
   ClusterManager      SchedulingQueue            |
   (接入子集群)        (优先级队列)               |
          |                   |                   |
          |            +------+------+            |
            |          |             |            |
            v          v             |            |
      子集群 Informer  ScheduleOne    |            |
      (Node/Pod事件)   (调度循环)    |            |
            |          |             |            |
            |    +-----+-----+      |            |
            |    |           |      |            |
            |    v           v      |            |
            |  AssumePod  Dispatch  |            |
            |  (缓存预扣) (Karmada)  |            |
            |    |           |      |            |
            v    v           |      |            |
       Scheduler Cache       |      |            |
       (内存状态机)          |      |            |
            ^                |      |            |
            |                v      v            |
            +-------- 子集群 Pod 事件对账 --------+
```

**关键闭环**：调度器缓存中的资源数据，通过直连子集群的 Informer 实时更新；调度决策通过 Karmada 下发；下发结果通过子集群 Pod 事件回调进行对账，形成完整闭环。

---

## 六、面试表达要点

1. **问题驱动**：每个设计决策都要说清楚"遇到了什么问题" -> "K8s 是怎么做的" -> "我们做了什么适配"
2. **对比突出**：与 Karmada 原生调度的对比、与之前 MySQL 方案的对比、与传统指数退避的对比
3. **闭环思维**：预扣不是单向操作，要有对账和回滚机制；调度不是单次动作，要有事件驱动的持续优化
4. **性能意识**：内存缓存减少 IO、世代号增量快照减少拷贝、QueueingHint 减少无效重试、并发过滤提升吞吐